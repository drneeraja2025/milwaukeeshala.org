# Staff admin (/staff/login → /staff/publish) — set these in Vercel Production. Never commit real values.
CONTENT_PUBLISH_SECRET=
# Optional alias for the login password (falls back to CONTENT_PUBLISH_SECRET):
# ADMIN_PASSWORD=
GITHUB_CONTENT_TOKEN=
# Optional overrides:
# GITHUB_CONTENT_REPO=drneeraja2025/milwaukeeshala.org
# GITHUB_CONTENT_BRANCH=main
